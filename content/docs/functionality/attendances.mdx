---
title: Attendances
description: Retrieve and manage student attendances.
---

# Attendances

Retrieve your attendances and absence statistics using the `getAttendances` function.

```ts
import { getAttendances } from '@studentsphere/linksign';

const attendances = await getAttendances(instance);
console.log(attendances);
```
