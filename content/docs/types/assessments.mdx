---
title: Assessments
description: Types and interfaces for the assessments module.
---

import { TypeTable } from 'fumadocs-ui/components/type-table';

This section documents the types and interfaces available in the `assessments` module.

## `AssessmentTime` (Type)

```typescript
"future" | "past"
```

---

## `Assessment` (Interface)

```typescript
export interface Assessment {
  id: string;
  name: string;
  gradingType: string | null;
  deadlineDate: string;
  status: string;
  subject: string | null;
}
```

### Properties

<TypeTable
  type={{
    id: {
      type: `string`,
      description: "Unique identifier",
    },
    name: {
      type: `string`,
      description: "Name",
    },
    gradingType: {
      type: `string | null`,
      description: "Grading type",
    },
    deadlineDate: {
      type: `string`,
      description: "Deadline date",
    },
    status: {
      type: `string`,
      description: "Status",
    },
    subject: {
      type: `string | null`,
      description: "Subject",
    },
  }}
/>

---

## `AssessmentDetail` (Interface)

```typescript
export interface AssessmentDetail {
  handedInAt: string;
  description: string;
  link: string | null;
}
```

### Properties

<TypeTable
  type={{
    handedInAt: {
      type: `string`,
      description: "Handed in at",
    },
    description: {
      type: `string`,
      description: "Description",
    },
    link: {
      type: `string | null`,
      description: "Link",
    },
  }}
/>

---

## `Assessments` (Type)

```typescript
PaginatedData<Assessment>
```

---

