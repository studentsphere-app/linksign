---
title: Trainings
description: Types and interfaces for the trainings module.
---

import { TypeTable } from 'fumadocs-ui/components/type-table';

This section documents the types and interfaces available in the `trainings` module.

## `Training` (Interface)

```typescript
export interface Training {
  ID: string;
  NAME: string;
  START: string;
  END: string;
  ARCHIVED: number;
  GOALS: string;
  COURSES_COUNT: number;
}
```

### Properties

<TypeTable
  type={{
    ID: {
      type: `string`,
      description: "Unique identifier",
    },
    NAME: {
      type: `string`,
      description: "Name",
    },
    START: {
      type: `string`,
      description: "Start",
    },
    END: {
      type: `string`,
      description: "End",
    },
    ARCHIVED: {
      type: `number`,
      description: "Archived",
    },
    GOALS: {
      type: `string`,
      description: "Goals",
    },
    COURSES_COUNT: {
      type: `number`,
      description: "Courses count",
    },
  }}
/>

---

