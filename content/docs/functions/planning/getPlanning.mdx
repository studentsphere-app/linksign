---
title: getPlanning
description: Documentation for the getPlanning function.
---

import { TypeTable } from 'fumadocs-ui/components/type-table';

This page documents the `getPlanning` function from the `@studentsphere/linksign` library.

## Parameters

<TypeTable
  type={{
    token: {
      type: `string`,
      description: "Token",
    },
    start: {
      type: `string`,
      description: "Start",
    },
    end: {
      type: `string`,
      description: "End",
    },
    deviceId: {
      type: `string`,
      description: "Device id",
      optional: true,
    },
  }}
/>

## Usage

```typescript
import { getPlanning } from "@studentsphere/linksign";

async function run() {
  const result = await getPlanning('YOUR_TOKEN', 'start', 'end', 'OPTIONAL_DEVICE_ID');
  console.log(result);
}

run().catch(console.error);
```

## Return Value

Returns a `Promise` resolving to a **[PlanningEntry](/docs/types/planning#planningentry-type)[]** object.
